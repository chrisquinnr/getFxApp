<!doctype html>
<html lang="en-GB">
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <link rel="stylesheet" href="/site.css">
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-md-12 col-lg-6">
        <h1>Welcome to GetFx</h1>
        <p class="currencies">
          <div class="currency-flag currency-flag-<%= fromflag %>"></div>
          <span class="from"> <%= from %></span>
          to
          <div class="currency-flag currency-flag-<%= toflag %>"></div>
          <span class="to"> <%= to %> </span>
        </p>
        <p><%= amt %> <%= from %> in <%= to %> is:</p>
        <p class="output"> <%= sprintf("%.2f", output) %></h3>
        </div>
      </div>
      <div class="row">
        <div class="col-md-12 col-lg-12">
          Change currency:
          <select id='currencyList' onchange=reRun()>
            <% currlist.each do |currency| %>
              <option><%= currency %></option>
            <% end %>
          </select>
        </div>
      </div>
    </div>
    <script>
      function reRun() {
        var curr = document.getElementById("currencyList").value;
        window.location.href = 'http://localhost:4567/' + curr + '/2017-02-28/1';
      }
    </script>
  </body>
  </html>
